server:
  port: 9090

tsanet:
  api:
    base-url: "http://localhost:8080"
  auth:
    username: "api@appko.com"
    password: "T123456!"
  storage:
    sqlite-path: "${user.home}/.tsanet-client-demo/data.db"
  connect-db:
    url: "jdbc:postgresql://localhost:5432/tsa"
    username: "admin"
    password: "admin"
  demo:
    setup:
      enabled: true
  scenarios:
    run-on-startup: true
    acme:
      username: "api@appko.com"
      password: "T123456!"
    beta:
      # Must be a Connect-1 account (login is proxied). Mapped to Beta via DB setup (users.company_id = 2).
      username: "admin@tsanet.org"
      password: "T123456!"
  sync:
    enabled: true
    initial-delay-ms: 10000
    interval-ms: 60000
    poll-scenario-accounts: true
